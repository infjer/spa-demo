export class Module {
    constructor(options) {

    }
    build(options) {

    }
    show(context) {
        if(this.body && this[parent]) {
            this[parent].appendChild(this.body);
        }
    }
    refresh(context) {

    }
    hide() {
        if(this.body) {
            fragment.appendChild(this.body);
        }
    }
    destroy() {
        
    }
}
